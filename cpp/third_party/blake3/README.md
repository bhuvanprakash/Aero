# BLAKE3 C Implementation

The BLAKE3 C sources are fetched automatically via CMake `FetchContent`
from the official [BLAKE3 repository](https://github.com/BLAKE3-team/BLAKE3).

No manual vendoring is needed. See the root `CMakeLists.txt` for the
pinned tag and build configuration.
