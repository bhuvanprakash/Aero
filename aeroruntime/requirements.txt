# aero-runtime dependencies
# Base (no inference): pip install -r requirements.txt && pip install -e .
# With inference:     pip install -e ".[inference]" or use scripts/install_runtime.py from repo root

aerotensor>=0.2.0
numpy>=1.20
gguf>=0.6

# Optional [inference] extra (for aero-run generation and AeroModel):
# torch>=2.0
# transformers>=4.30
