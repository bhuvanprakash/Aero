"""AERO meta package — installs aerotensor and aero-runtime[inference].

This package has no code; it only declares dependencies so that
`pip install aero` installs the format library and runtime in one step.
"""

__version__ = "0.2.0"
